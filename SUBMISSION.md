# מסמך הגשה — מערכת בלוג

**סטודנט:** עומרי דיגמי

---

## 1. מיפוי דרישות למימוש

| # | דרישת המנחה | היכן מומש | סטטוס |
|---|---|---|---|
| 1 | PostgreSQL | `config/settings.py` → `DATABASES` | ✅ |
| 2 | Django REST Framework | `INSTALLED_APPS`, כל ה-Views | ✅ |
| 3 | djangorestframework-simplejwt | `accounts/urls.py`, `SIMPLE_JWT` | ✅ |
| 4 | django-cors-headers | `MIDDLEWARE`, `CORS_ALLOWED_ORIGINS` | ✅ |
| 5 | django-filter | `articles/filters.py` → `ArticleFilter` | ✅ |
| 6 | python-decouple + python-dotenv | `config/settings.py`, `.env` | ✅ |
| 7 | חיפוש: כותרת, תוכן, תגיות, שם מחבר | `ArticleViewSet.search_fields` | ✅ |
| 8 | זריעה: 5+ משתמשים, 8 כתבות, 2-3 תגובות | `seed_db.py` — 6 / 8 / 18 | ✅ |
| 9 | תיעוד מלא ב-Browsable API | `/api/` + docstrings + `help_text` | ✅ |
| 10 | סודות במשתני סביבה | `.env` + `.gitignore` | ✅ |
| 11 | שלוש קבוצות הרשאה | `create_groups.py` | ✅ |
| 12 | צד לקוח React | `frontend/` | ✅ |
| 13 | סרגל ניווט: הרשמה / התחברות / התנתקות | `components/Navbar.jsx` | ✅ |
| 14 | 3 כתבות אחרונות + טעינת ישנות + חיפוש | `pages/Home.jsx` | ✅ |
| 15 | עמוד כתבה + תגובות + עריכה/מחיקה לבעלים | `pages/ArticleDetail.jsx` | ✅ |

---

## 2. הרצת המערכת

**טרמינל 1 — שרת:**

```bash
cd ~/Desktop/Projects/hackeru-finalproject
source venv/bin/activate
python manage.py runserver
```

**טרמינל 2 — צד לקוח:**

```bash
cd ~/Desktop/Projects/hackeru-finalproject/frontend
npm run dev
```

| כתובת | תיאור |
|---|---|
| http://localhost:5173 | ממשק המשתמש |
| http://127.0.0.1:8000/api/ | תיעוד ה-API (Browsable API) |
| http://127.0.0.1:8000/admin/ | ממשק הניהול |

**איפוס והרצה מאפס:**

```bash
python manage.py migrate
python manage.py create_groups
python manage.py seed_db
python manage.py createsuperuser
```

---

## 3. משתמשי הדגמה

סיסמה זהה לכולם: `Demo!Pass2026`

| משתמש | קבוצה | מה אפשר להדגים איתו |
|---|---|---|
| `boss` | managers | עריכה ומחיקה של כל כתבה, מחיקת תגובות של אחרים |
| `editor1` | editors | יצירת כתבה, עריכת הכתבות שלו בלבד |
| `editor2` | editors | ניסיון לערוך כתבה של editor1 → 403 |
| `dana` | users | כתיבת תגובה, עריכה ומחיקה של התגובה שלה |
| `yossi`, `maya` | users | ניסיון לערוך תגובה של dana → 403 |

---

## 4. תרחישי הדגמה מומלצים

### א. חיפוש בארבעה שדות

בכתובת http://127.0.0.1:8000/api/articles/

```
?search=גינת          → לפי כותרת
?search=עציצים        → לפי תוכן
?search=תחביבים       → לפי תגית (4 תוצאות)
?search=editor1       → לפי שם המחבר (4 תוצאות)
```

**נקודה מעניינת להדגמה:** `?search=קפה` מחזיר **שתי** כתבות — זו על הקפה, וזו על
הטיול בגליל שמזכירה "קפה בתרמוס". זה מוכיח שהחיפוש סורק גם את גוף הכתבה
ולא רק את הכותרת ואת התגיות.

### ב. סינון מדויק (django-filter)

```
?tag=טיולים
?author=editor2
?published_after=2026-01-01
?tag=תחביבים&ordering=title      ← שילוב סינון ומיון
```

### ג. הרשאות — להראות שהשרת אוכף, לא רק הממשק

1. התחבר כ-`editor2` ב-Browsable API
2. נסה `PATCH` על כתבה של `editor1` → **403 Forbidden**
3. התחבר כ-`boss` ונסה שוב → **200 OK**

וכן:

1. התחבר כ-`yossi`, נסה למחוק תגובה של `dana` → **403**
2. `dana` מוחקת את התגובה שלה → **204**
3. `boss` מוחק כל תגובה → **204**

### ד. צד לקוח

1. עמוד ראשי — 3 כתבות בלבד + כפתור "הצג כתבות ישנות יותר"
2. הקלדה בשורת החיפוש — התוצאות מתעדכנות אחרי השהיה קצרה
3. כניסה לכתבה — תגובות מלאות
4. התחברות כ-`dana` — כפתורי עריכה ומחיקה מופיעים **רק** על התגובות שלה
5. התנתקות — הכפתורים נעלמים

---

## 5. החלטות תכן שכדאי להסביר

**מודל User מותאם מההתחלה.** הוגדר `AUTH_USER_MODEL` לפני המיגרציה הראשונה.
שינוי מאוחר יותר היה מחייב איפוס מלא של מסד הנתונים.

**המחבר נקבע בשרת, לא בלקוח.** גם אם הלקוח שולח שדה `author`, השרת מתעלם
ממנו ומשתמש ב-`request.user`. אחרת אפשר היה לפרסם כתבה בשם מישהו אחר.

**מנהל מוחק תגובות אך אינו עורך אותן.** מחיקה נדרשת להסרת תוכן לא רצוי,
אך עריכה הייתה מאפשרת לשים מילים בפיו של משתמש.

**`has_object_permission` אינו נקרא ביצירה.** לכן בדיקת ההרשאה ליצירת כתבה
נמצאת ב-`has_permission`. זו טעות נפוצה שגורמת לחור אבטחה.

**`PAGE_SIZE=3`.** מייצר את "3 הכתבות האחרונות" בעמוד הראשי ללא קוד ייעודי,
וכפתור הטעינה פשוט מבקש `?page=2`.

**תגיות כשדה טקסט מנורמל.** בשמירה `" Django , API , django "` הופך ל-`django,api`.
בלי הנרמול, חיפוש לפי תגית היה מפספס בגלל רווחים ואותיות גדולות.

**`select_related` ו-`prefetch_related`.** מונעים בעיית N+1 — ללא זה, הצגת
רשימת כתבות הייתה מייצרת שאילתה נפרדת לכל מחבר ולכל תגובה.

**`.update()` בזריעה.** `published_at` מוגדר `auto_now_add`, ולכן כל 8 הכתבות
היו מקבלות את אותה שנייה. `update()` עוקף זאת ומדרג את התאריכים, כך
ש"3 האחרונות" מקבל משמעות.

---

## 6. אבטחה

- `SECRET_KEY` וסיסמת מסד הנתונים נטענים מ-`.env` באמצעות `python-decouple`
- `.env` מוחרג ב-`.gitignore` ואינו נכנס למאגר הקוד
- `.env.example` עם ערכי דמה כן נכנס, כדי שאפשר יהיה לשחזר את הסביבה
- סיסמאות נשמרות מוצפנות בלבד (`create_user` / `set_password`)
- ולידציית סיסמאות של Django פעילה בהרשמה
- `CorsMiddleware` ממוקם לפני `CommonMiddleware`, ורק מקורות מאושרים
  מקבלים כותרת CORS

---

## 7. מה לא מומש ולמה

| נושא | סיבה |
|---|---|
| endpoint של logout | ב-JWT ההתנתקות היא מחיקת הטוקן בצד הלקוח. אין מצב שמור בשרת |
| ניהול כתבות בצד הלקוח | לא נכלל בדרישות הפרונטאנד. מתבצע דרך ה-Browsable API או ה-Admin |
| מחיקת משתמשים בצד הלקוח | מתבצעת דרך ממשק הניהול של Django |
| העלאת תמונות | לא נדרש באפיון |
| בדיקות אוטומטיות | לא נדרש. הבדיקות בוצעו ידנית ומתועדות בסעיף 4 |
